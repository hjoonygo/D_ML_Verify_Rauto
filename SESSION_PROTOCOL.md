# 세션 라이프사이클 프로토콜 (지침 단일출처) — 캡틴 지시 2026-06-25

> 캡틴 격노 배경: AI가 세션 시작 시 세션명도 안 묻고 첫 작업에 손댐("내가 우습냐"). + 작업 중 큰 통찰을 그냥 흘려보냄.
> → **세션이 시작되면 Claude가 '자동으로' 아래 3관문을 확인·이행한다.** CLAUDE.md가 자동로드되므로 매 세션 이 절을 펼쳐 따른다.
> bypassPermissions(야간 무인)/Auto mode여도 1·2·3은 건너뛰지 않는다. (선택: SessionStart 훅으로 화면 리마인드 추가 가능.)

## 관문 1 — 세션 START (첫 산출물 만들기 '직전')
1. **§18 연속성 브리핑**: 제작노트(01_제작노트: 참사5단계·끊김없이다음할것·개발아이디어·MOM허브)를 Work Order·Handover·KeyNote·INDEX·STATE와 크로스체크 → "오늘 이어서 할 것 + 미결 아이디어 + 참사 교훈" 1묶음 보고.
2. **★세션명 제안·확인 (필수, 누락=위반)**: "이번 세션명을 `___`로 제안합니다 — 확정/회차는?" 1줄 질문.
   - 세션ID = `YYMMDD_회차_세션명`.
   - **회차 = '이번 세션에서 몇 번째 결과물 작업이냐'(AI가 세서 정함)**. ★직전 세션 번호를 잇는 게 아니다(캡틴 정정 2026-06-25). 이 세션의 첫 작업그룹=01.
   - 세션명 = 짧은 영문 CamelCase 2~3단어(그 세션 핵심).
   - 캡틴 확정 후에만 그 세션ID로 명명·저장 시작. (PIPELINE_GradeStepUp.md §1 + CLAUDE.md §16·confirm-filename)

## 관문 2 — 세션 DURING (작업 중, ★신설 — 능동 통찰포착)
- **큰 결과·통찰을 Claude가 *스스로* 판단해 발견하면**(전환점·새 알파·중요 버그·검증 반전 등), 캡틴이 강조어를 말하지 않아도 **능동적으로**:
  1. 해당 문서 작성/갱신 — 기술상세=`KeyNote_*` / 방법론·TIL=`Guide_*` / 미래과제=`Work_Order_*` / 분야로직=`LogicCatalog`. (라우팅 = CLAUDE.md §13)
  2. **만들기 전 네이밍을 1줄 묻는다**(§16·confirm-filename) — "이 통찰을 `KeyNote_(세션ID)_(작업명).docx`로 남기려는데 이름 괜찮나요?"
  3. 전환점이면 §14 6단계 + 5곳 동시기재.
- 판단 기준(통찰=기록대상): 알파/비용/리스크/아키텍처/운영 '기준'이 바뀌거나, 재현·검증으로 새 사실이 확정될 때.

## 관문 3 — 세션 END (마무리)
- **인수인계 지침(§17) 전체 이행**: 문서 7종(Handover·KeyNote·Guide·Basic·Hstr·LogicCatalog·Work_Order 해당분) + 기록 4종(INDEX·STATE·CLAUDE 전환점·memory) + 재현 zip 1개 + 전환점 자문 + 누락 자가검증(①~⑪ 한 줄씩 '했음/해당없음').
- 파일명은 만들기 전 확인(§16), 저장 후 위치 1줄 보고(§12), D:+G 둘 다.

## 변경이력
- 2026-06-25 신설. 캡틴 지시(세션명 미확인 격노 + 통찰 능동포착 + 핸드오버를 세션 시작지침으로 묶기). 단일출처=본 문서, CLAUDE.md §21이 참조.
