================================================================================
TIL 후보 — 데이터 계보 분기 함정 (2026-06-12, 08Prj_Dauto_Ch2_Stg13 핀포인트)
================================================================================
[한 줄] 같은 컬럼명이라도 생성 수식이 버전(파이프라인 단계)마다 다를 수 있다.
        채택 기준은 '스크립트 발견'이 아니라 '기준 데이터와의 전수 수치 일치'다.

[사건 전말]
- oi_zscore_24h 라이브 어댑터를 만들기 위해 생성 수식을 추적.
- 캡틴이 원본 스크립트(compute_oi_derived_features.py v2, 33ecde59)를 제공 —
  줄번호·수식 §2 증거까지 전부 사실이었다. 그런데 전수 대조하니 실사용
  Merged_Data.csv와 값불일치 311,819행: 이 스크립트는 '중간 산출물(Derived 05-07)'의
  생성자였고, 실사용본(REPAIRED 05-11)은 그 후 결측복구 단계에서 z가
  '다른 수식'(z 전체 1봉 지연·min_periods 720)으로 재계산돼 있었다.
- 역공학(96후보 격자 + NaN 패턴 단서)으로 REPAIRED 수식을 전수 0불일치로 핀포인트.
  재계산 스크립트 원본은 끝내 미발견 — 그래도 채택 가능했던 근거는 '수치 전수 일치'.

[교훈 3개]
1) 원본 스크립트 != 데이터의 진짜 계보. 파이프라인 단계(생성→복구→병합)마다
   같은 컬럼이 재계산될 수 있다. 스크립트를 믿지 말고 데이터에 대조하라.
2) NaN 패턴은 공짜 단서다(워밍업 720행 → min_periods=720 핀포인트,
   경계값 -10.0 정밀일치 → 클리핑 존재 판정).
3) 라이브 공급원의 채택 기준 = '백테스트가 본 그 값'의 재현(알파 검증 사슬 유지).
   더 '올바른' 수식이 있어도 검증 사슬 밖이면 전 알파 재검증 비용이 든다.

[기록] LINEAGE_WARNING_oi_zscore.txt(D:\ML + Stg13 + G복구폴더) / CLAUDE.md §8 주석 /
       SidewayDCA·TrendStack_CLAUDE.md 1줄 / 분석 202606121121.txt
================================================================================
