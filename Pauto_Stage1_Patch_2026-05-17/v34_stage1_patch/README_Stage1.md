# Pauto v3.4 Stage 1 — 정정 패치 (Round 2)

## 이전 측정 결과 분석

사용자 PC cmd 출력 직접 정독:

| 사실 | 상태 |
|---|---|
| 학습 라벨 분포 | stay 39% / long 30% / short 30% — **완벽 균형** (ⓟ-6 정정 성공) |
| Regime 분포 (TF 15m) | BULLISH 27% / BEARISH 26% / CHOPPY 47% (★ S1 정정 성공) |
| Regime 분포 (TF 1h) | BULLISH 26% / BEARISH 26% / CHOPPY 47% (★ S1 정정 성공) |
| **Long/Short 신호** | **0건 / 0건** — 임계 0.50이 3-class 모델에 너무 높음 |
| 학습 인자 사용 | **없음** — 전체 36mo 학습. 점프 ⓟ-9 재발생 |
| measure 빈 결과 처리 | KeyError 'adr_w3_passed' 발생 |

## 본인 점프 정직 인정 (4건)

1. **점프 21**: 합성 검증 시 Long 0건 사실 행간 분석 안 함
2. **점프 22**: README에 학습 인자 없이 실행 안내 → 점프 ⓟ-9 재발
3. **점프 23**: measure_stage1.py 빈 결과 방어 코드 없음
4. **점프 24**: 임계 0.50을 binary 감각으로 결정 — 3-class 평균 0.33 대비 너무 높음

## 본 패키지 정정 사항

| 파일 | 변경 |
|---|---|
| `diagnose_prob_distribution.py` (신규) | 진짜 prob 분포 30초 측정 → 적합 임계 권장 |
| `measure_stage1.py` 정정 | 점프 23 방어 코드 + 임계 CLI 인자화 |
| `README_Stage1.md` 정정 | 점프 22 정정 — ⓟ-9 명시 |

## 작업 순서 (전체 약 1.5~5시간)

### Step 1 — 본 패키지 풀고 덮어쓰기

```cmd
cd D:\ML\Verify\Pauto_Stage1_2026-05-17
:: 새 zip 풀어서 diagnose_prob_distribution.py + measure_stage1.py + README 덮어쓰기
```

### Step 2 — 진단 (약 30초)

```cmd
python diagnose_prob_distribution.py
```

출력 핵심:
- `[임계별 신호 빈도 추정]` 표
- `[권장 임계]` 한 줄

★ 두 가능성:
- 권장 임계 0.40~0.45 → 임계만 변경하면 됨
- 권장 임계 0.30 이하 → 모델 자체 문제 가능. 본인에게 보고

### Step 3 — 재학습 (★ 사용자 결정)

현재 모델은 36mo 전체 학습 = 점프 ⓟ-9 재발.

선택:
- (a) **재학습 24mo (권장)**:
  ```cmd
  python ML_Predictor_Pipeline_PautoV75.py 2023-05-01 2025-04-30
  ```
  약 1~2분. 학습 후 진단 재실행.

- (b) 현재 36mo 모델 사용 (ⓟ-9 무시)

### Step 4 — 측정 (예상 1~4시간)

```cmd
:: 진단 결과 권장 임계가 0.40인 경우
python measure_stage1.py 0.40
```

### Step 5 — 결과 압축 + 업로드

```cmd
:: outputs_stage1/ 통째로 zip → Claude 업로드
```

## 본인 진단 시간 예상

| 단계 | 시간 |
|---|---|
| zip 풀기 | 30초 |
| 진단 | 30초 |
| 재학습 (선택) | 1~2분 |
| 진단 재실행 (재학습 시) | 30초 |
| 측정 | 1~4시간 |
| **총** | **1~4시간 + α** |

## 가능한 결과 시나리오

**A**: 진단 권장 0.40 + 재학습 안 함 → 신호 발생하나 ⓟ-9 의심
**B (★ 권장)**: 진단 권장 0.40 + 재학습 24mo → 결과 신뢰도 90%
**C**: 진단 권장 0.30 이하 → 모델 자체 문제. 본인에게 보고
**D**: 어떤 임계로도 알파 없음 → PautoV75 ML 폐기 또는 22 자원 활용

## 알파 판정 (ADR-W3 그대로)

- PF ≥ 1.3, n_valid ≥ 30, net_return_sum > 0
- 4 장세 중 최소 2장세 PF ≥ 1.3

## 본인 미해결 우려

- 진단 시간 추정 30초: 신뢰도 70%
- Stage 1 측정 1~4시간: 신뢰도 30%
- 권장 임계 0.40 가설: 진단 결과로 결정
